{{- if .Values.ha_enabled }}
# -------------------------------
# Etcd Headless Service (HA peer discovery)
# -------------------------------
apiVersion: v1
kind: Service
metadata:
  name: {{ include "etcd.headlessName" . }}
  namespace: {{ include "etcd.namespace" . }}
  labels:
    {{- include "etcd.labels" . | nindent 4 }}
    etcd.service: headless
    {{- with .Values.commonLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.serviceAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  clusterIP: None
  publishNotReadyAddresses: true
  selector:
    {{- include "etcd.selectorLabels" . | nindent 4 }}
  ports:
    - name: peer
      port: {{ .Values.peerPort }}
      targetPort: {{ .Values.peerPort }}
    - name: client-grpc
      port: {{ .Values.grpcPort }}
      targetPort: {{ .Values.grpcPort }}

---
# -------------------------------
# Etcd StatefulSet (HA)
# -------------------------------
apiVersion: apps/v1
kind: StatefulSet
metadata:
  name: {{ include "etcd.fullname" . }}
  namespace: {{ include "etcd.namespace" . }}
  labels:
    {{- include "etcd.labels" . | nindent 4 }}
    {{- with .Values.commonLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.commonAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  replicas: {{ .Values.replicas }}
  serviceName: {{ include "etcd.headlessName" . }}
  podManagementPolicy: Parallel
  {{- if .Values.pvcRetentionPolicy.enabled }}
  persistentVolumeClaimRetentionPolicy:
    {{- if .Values.pvcRetentionPolicy.whenDeleted }}
    whenDeleted: {{ .Values.pvcRetentionPolicy.whenDeleted }}
    {{- end }}
    {{- if .Values.pvcRetentionPolicy.whenScaled }}
    whenScaled: {{ .Values.pvcRetentionPolicy.whenScaled }}
    {{- end }}
  {{- end }}
  selector:
    matchLabels:
      {{- include "etcd.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "etcd.selectorLabels" . | nindent 8 }}
        {{- with .Values.podLabels }}
        {{- toYaml . | nindent 8 }}
        {{- end }}
      {{- with .Values.podAnnotations }}
      annotations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
    spec:
      containers:
        - name: etcd
          image: {{ .Values.image }}
          env:
            - name: POD_NAME
              valueFrom:
                fieldRef:
                  fieldPath: metadata.name
          command:
            - etcd
          args:
            - --data-dir=/var/lib/etcd
            - --name=$(POD_NAME)
            - --listen-peer-urls=http://0.0.0.0:{{ .Values.peerPort }}
            - --listen-client-urls=http://0.0.0.0:{{ .Values.grpcPort }}
            - --listen-client-http-urls=http://0.0.0.0:{{ .Values.httpPort }}
            - --advertise-client-urls=http://$(POD_NAME).{{ include "etcd.headlessName" . }}:{{ .Values.grpcPort }}
            - --initial-advertise-peer-urls=http://$(POD_NAME).{{ include "etcd.headlessName" . }}:{{ .Values.peerPort }}
            - --initial-cluster={{ include "etcd.initialCluster" . }}
            - --initial-cluster-state=new
          ports:
            - name: client-grpc
              containerPort: {{ .Values.grpcPort }}
            - name: client-http
              containerPort: {{ .Values.httpPort }}
            - name: peer
              containerPort: {{ .Values.peerPort }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
          {{- if .Values.healthChecks.enabled }}
          livenessProbe:
            httpGet:
              path: {{ .Values.healthChecks.liveness.path }}
              port: {{ .Values.httpPort }}
            initialDelaySeconds: {{ .Values.healthChecks.liveness.initialDelaySeconds }}
            periodSeconds: {{ .Values.healthChecks.liveness.periodSeconds }}
            timeoutSeconds: {{ .Values.healthChecks.liveness.timeoutSeconds }}
            failureThreshold: {{ .Values.healthChecks.liveness.failureThreshold }}
          readinessProbe:
            httpGet:
              path: {{ .Values.healthChecks.readiness.path }}
              port: {{ .Values.httpPort }}
            initialDelaySeconds: {{ .Values.healthChecks.readiness.initialDelaySeconds }}
            periodSeconds: {{ .Values.healthChecks.readiness.periodSeconds }}
            timeoutSeconds: {{ .Values.healthChecks.readiness.timeoutSeconds }}
            failureThreshold: {{ .Values.healthChecks.readiness.failureThreshold }}
          startupProbe:
            httpGet:
              path: {{ .Values.healthChecks.startup.path }}
              port: {{ .Values.httpPort }}
            periodSeconds: {{ .Values.healthChecks.startup.periodSeconds }}
            timeoutSeconds: {{ .Values.healthChecks.startup.timeoutSeconds }}
            failureThreshold: {{ .Values.healthChecks.startup.failureThreshold }}
          {{- end }}
          volumeMounts:
            - name: data
              mountPath: /var/lib/etcd
  volumeClaimTemplates:
    - metadata:
        name: data
        {{- with .Values.commonLabels }}
        labels:
          {{- toYaml . | nindent 10 }}
        {{- end }}
        {{- with .Values.pvcAnnotations }}
        annotations:
          {{- toYaml . | nindent 10 }}
        {{- end }}
      spec:
        accessModes:
          - ReadWriteOnce
        resources:
          requests:
            storage: {{ .Values.storage.size }}
        {{- if .Values.storage.storageClassName }}
        storageClassName: {{ .Values.storage.storageClassName }}
        {{- end }}

---
# -------------------------------
# Etcd Client Service
# -------------------------------
apiVersion: v1
kind: Service
metadata:
  name: {{ include "etcd.fullname" . }}
  namespace: {{ include "etcd.namespace" . }}
  labels:
    {{- include "etcd.labels" . | nindent 4 }}
    etcd.service: client
    {{- with .Values.commonLabels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  {{- with .Values.serviceAnnotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.service.type | default "ClusterIP" }}
  selector:
    {{- include "etcd.selectorLabels" . | nindent 4 }}
  ports:
    - name: client-grpc
      port: {{ .Values.grpcPort }}
      targetPort: {{ .Values.grpcPort }}
    - name: client-http
      port: {{ .Values.httpPort }}
      targetPort: {{ .Values.httpPort }}
{{- end }}
